import styled from 'styled-components';

export const Div = styled.div`
  background-color: #FFF;
  box-shadow: 0 0 30px rgba(0, 0, 0, 0.3);
  border-radius: 5% 2% 0;
  z-index: 1;

  grid-area: card-top / card-left / card-bottom / card-right;
`